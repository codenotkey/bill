type RecordItem = {
    spendOrIncome:string
    type: string
    notes: string
    amount: string // 数据类型 object | string
    createdAt?: Date  // 类 / 构造函数
}
type budgetData = {
    sTime:string,
    eTime:string,
    totalMoney:string,
    remainMoney:string

}
interface Window {
}
